server:
    port: 8089

spring:
    application:
        name: hw-response-service
    datasource:
        url: jdbc:mysql://localhost:3306/course_project_hw_responses
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8081/eureka/
