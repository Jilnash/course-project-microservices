server:
    port: 8095

spring:
    application:
        name: course-service-saga
    kafka:
        bootstrap-servers: localhost:9092

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8081/eureka/